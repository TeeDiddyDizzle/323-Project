%%
     int    low,  high, step$;      ! declarations !

     get (low, high, step$);
     while (low + 1  <  high )  
         {  put (low);
             put (low);
             low =  low  +  step$;
         } 
