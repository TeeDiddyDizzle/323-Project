function add [first: real, second:real] {
    return first + second;
}

function subtract [first:real, second: real] {
    return first - second;
}

%% 

real total;
real first, second;

! call functions!
get(first, second);

add(first, second);
subtract(first, second);